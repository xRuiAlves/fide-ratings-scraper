/**
 * Resource types
 */
const RESOURCE_TYPES = Object.freeze({
    GET: {
        INFO: "info",
        PERSONAL_DATA: "personal-data",
        RANK: "rank",
        ELO: "elo",
        HISTORY: "history",
    },
});

module.exports = RESOURCE_TYPES;
